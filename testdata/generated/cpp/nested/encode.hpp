/* encode.hpp - Encoding functions for nested
 * Generated by sdp-gen - DO NOT EDIT
 */

#ifndef NESTED_ENCODE_HPP
#define NESTED_ENCODE_HPP

#include "types.hpp"
#include <cstdint>
#include <cstddef>

namespace sdp {

/* Calculate encoded size of Point */
size_t point_size(const Point& msg);

/* Encode Point to buffer
 * Returns: Number of bytes written
 * Note: Buffer must be at least point_size() bytes
 */
size_t point_encode(const Point& msg, uint8_t* buf);

/* Calculate encoded size of Rectangle */
size_t rectangle_size(const Rectangle& msg);

/* Encode Rectangle to buffer
 * Returns: Number of bytes written
 * Note: Buffer must be at least rectangle_size() bytes
 */
size_t rectangle_encode(const Rectangle& msg, uint8_t* buf);

/* Calculate encoded size of Scene */
size_t scene_size(const Scene& msg);

/* Encode Scene to buffer
 * Returns: Number of bytes written
 * Note: Buffer must be at least scene_size() bytes
 */
size_t scene_encode(const Scene& msg, uint8_t* buf);

}  // namespace sdp

#endif  // NESTED_ENCODE_HPP
