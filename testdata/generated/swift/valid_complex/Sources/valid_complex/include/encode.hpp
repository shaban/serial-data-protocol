/* encode.hpp - Encoding functions for valid_complex
 * Generated by sdp-gen - DO NOT EDIT
 */

#ifndef VALID_COMPLEX_ENCODE_HPP
#define VALID_COMPLEX_ENCODE_HPP

#include "types.hpp"
#include <cstdint>
#include <cstddef>

namespace sdp {

/* Calculate encoded size of DeviceList */
size_t device_list_size(const DeviceList& msg);

/* Encode DeviceList to buffer
 * Returns: Number of bytes written
 * Note: Buffer must be at least device_list_size() bytes
 */
size_t device_list_encode(const DeviceList& msg, uint8_t* buf);

/* Calculate encoded size of Device */
size_t device_size(const Device& msg);

/* Encode Device to buffer
 * Returns: Number of bytes written
 * Note: Buffer must be at least device_size() bytes
 */
size_t device_encode(const Device& msg, uint8_t* buf);

/* Calculate encoded size of Parameter */
size_t parameter_size(const Parameter& msg);

/* Encode Parameter to buffer
 * Returns: Number of bytes written
 * Note: Buffer must be at least parameter_size() bytes
 */
size_t parameter_encode(const Parameter& msg, uint8_t* buf);

}  // namespace sdp

#endif  // VALID_COMPLEX_ENCODE_HPP
